type User {
	id: Int!
	userName: String!
	email: String!
	teams: [Team!]!
}

type Query {
	getUser(id: Int!): User!
	allUsers: [User!]!
}

type Mutation {
	createUser(userName: String!, email: String!, password: String!): User!
}
